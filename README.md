# General

Misc utilities and notes.

## Contents

- `auto_network_discovery.py` — Python 3 network auto-discovery script
- `auto_network_discoveryv2.py` — v2 with defined network edges
- `Windows HDR` — notes on enabling and configuring HDR in Windows
- `DNS Rule of 3.docx` — DNS reference document

## Usage

Run the discovery scripts with Python 3, e.g. `python3 auto_network_discoveryv2.py`.
